name: Commit Changes

runs:
  using: composite
  steps:
    - shell: bash
      run: |
        git config user.name "github-actions"
        git config user.email "github-actions@github.com"

        git add apps/$SAFE_APP Keystore_History.md
        git commit -m "🔐 Keystore update for $SAFE_APP" || echo "No changes to commit"
        git push
