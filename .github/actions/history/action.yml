name: Update Keystore History

runs:
  using: composite
  steps:
    - shell: bash
      run: |
        FILE="Keystore_History.md"

        if [ ! -f "$FILE" ]; then
          echo "# 🔐 Keystore History" > $FILE
          echo "" >> $FILE
          echo "| Date | App | Action | Actor |" >> $FILE
          echo "|------|-----|--------|-------|" >> $FILE
        fi

        echo "| $(date '+%Y-%m-%d %H:%M') | $SAFE_APP | CREATE/UPDATE | $GITHUB_ACTOR |" >> $FILE
